/*
var arrayNum = [1, 2, 3, 4, 5];

for (var i = 0; i < 3; i++) {
    const element = arrayNum[i];
    console.log(element);
}
*/

/*
function encontrarLetra(string) {
    var letras = string.split('');
    for (let i = 0; i < letras.length; i++) {
        if (letras[i] === 'p') {
            console.log('Si contiene la letra P');
        }
    }
}

//encontrarLetra('Nop');
encontrarLetra('Pedro')
*/

var arr = [];

while (arr.length < 5) {
    arr.push('BOOM');
}

console.log(arr);