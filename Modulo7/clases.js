/*
function Auto(puertas, color, marca, año, ruedas) {
    this.puertas = puertas;
    this.color = color;
    this.marca = marca;
    this.año = año;
    this.ruedas = ruedas;

    this.info = function(){
        console.log('Este es un ' + this.marca + ' de color ' + this.color + ' Del año ' + this.año);
    }
}

var miPrimerAuto = new Auto(2, 'rojo', 'ferrari', 2018, 4);

console.log(miPrimerAuto);
console.log(miPrimerAuto.marca);
miPrimerAuto.info();
*/

/*
class Auto{
    constructor(puertas, color, marca, año, ruedas) {
        this.puertas = puertas;
        this.color = color;
        this.marca = marca;
        this.año = año;
        this.ruedas = ruedas;
    }

    info(){
        console.log('Este es un ' + this.marca + ' de color ' + this.color + ' Del año ' + this.año);
    }
}

var miSegundoAuto = new Auto(4, 'Blanco', 'Bugatti', 2023, 4)

console.log(miSegundoAuto);
console.log(miSegundoAuto.marca);
miSegundoAuto.info()
*/

/**
 * 
class Football{
    constructor(jugadorv){
        this.jugador = jugador;
    };

    obtenerNombre(){
        console.log(this.jugador);
    };
};

var argentina = new Football('Messi');
var brasil = new Football('Pele');

argentina.obtenerNombre();
brasil.obtenerNombre();
*/ 

/** */

